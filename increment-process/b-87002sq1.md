# approve the changesets release PR from the terminal

d-qzpfyc6s covers the increment's own pull request only. The changesets "Version Packages" PR in twin-digital/opus is a separate rubber stamp, blocked the same way: opus's require-reviews ruleset carries a pull_request rule on main with no bypass actor, so CI can neither push a version bump directly nor merge the release PR itself.

Alternatives priced and set aside 2026-08-05: the agents' App as a bypass actor (rejected -- agents would gain merge rights on any PR they open); a second CI-only App as bypass actor (correct scope, one-time cost of App registration, a KMS key, repo vars). Reaching the release PR from the land command's credential prompt may make both unnecessary.
