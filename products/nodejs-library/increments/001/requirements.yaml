version: "1"
requirements:
  - id: r-ds3q3en9
    title: node libraries are ESM TypeScript
    statement: |
      a Node.js library ships as ESM only, written in TypeScript and shipping its own type
      declarations, targeting active Node LTS.
    verification:
      - observe: the published package
        expect: '"type": "module" declared, no CommonJS entry, type declarations included, and the build target within active Node LTS'
