version: "1"
questions:
  - id: q-m1m99bfk
    question: |
      which store backs the backlog: a dedicated branch of markdown files — in-tree,
      greppable, one commit per capture — or GitHub issues — no machinery, native UI, one
      issue per item? the tooling surface hides the store either way.
    answer: decision
    facets:
      - tooling
      - process-doc
