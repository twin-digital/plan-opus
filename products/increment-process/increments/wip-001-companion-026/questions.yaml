version: "1"
questions:
  - id: q-btui8gxj
    question: >
      does r-rn6wxdn4 — every command takes `--json` — reach the interactive commands? seven of
      the cli's nine top-level commands have a machine-readable main output to render.
      `increment` is a full-screen session whose main output is the screen, and `land` is a step
      sequence written for a person; neither has a structured value behind it. the requirement's
      subject is what settles whether those two accept the flag as a no-op, refuse it, or fall
      outside it.
    answer: requirement
    facets:
      - tooling
