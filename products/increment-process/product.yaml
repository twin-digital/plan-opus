version: "1"
kind: process
facets:
  - id: process-doc
    description: the normative documents that define the process itself
  - id: schema
    description: the shapes of the structured files products write
  - id: design-validator
    description: the rules that check artifacts and gate merges
  - id: tooling
    description: the tools that parse, fold, display, and export the structured plan data
  - id: prompts
    description: instruction to agents working inside the process
