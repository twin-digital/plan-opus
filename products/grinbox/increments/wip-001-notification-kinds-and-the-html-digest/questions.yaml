version: "1"

questions:
  - id: q-mf8fl0ls
    question: |
      What do mail clients actually do with a digest's rich rendition — are style blocks and
      external stylesheets dropped, are remote images blocked by default, and do clients apply
      their own dark-mode transforms? d-zkb393z9 rests on all three and nothing records them.
    answer: fact
