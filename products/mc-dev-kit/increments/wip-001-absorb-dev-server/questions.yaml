version: "1"

# What the dev loop still rests on and cannot answer from the fold. The stranded cycle's five have
# all closed: three asked what the kit's API does, which inside this product is the kit's own
# requirements, and the two server questions closed against evidence/minecraft/dev-server —
# dev-loop-probe and reload-detection-probe, run 2026-08-05.
questions:
  - id: q-h34y88go
    question: |
      Does a resource pack deployed into `/data/development_resource_packs` and named in the world's
      `world_resource_packs.json` actually activate? f:bedrock-resource-packs-mirror-the-behavior-pack-layout
      establishes the pool path, the list's name and location, its entry shape, and that the server
      leaves the file byte-identical across a world load — but not that the pack loads. The Pack
      Stack line names behavior packs only, per f:pack-kind-mismatch-fails-silently-at-the-server,
      and a resource pack runs no script that could report itself. Closed by a probe with a client
      attached, or by a documented source. Gates d-4iepnry2 and the resource half of d-jv1zleaj;
      r-hpu39brj makes resource support optional, so a behavior-only first release does not wait on
      it.
    answer: fact
    facets: dev-loop
