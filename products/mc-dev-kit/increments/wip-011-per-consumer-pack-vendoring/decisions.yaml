version: "3"

decisions:
  - id: d-82zp77fo
    title: a vendored pack merges into the consumer's own pack
    statement: |
      a consumer's build writes the vendored pack's content into the consumer's own pack of that
      kind, under the consumer's namespace and its own header uuid. A consumer ships one behavior
      pack and one resource pack whatever it vendors.
    status: proposed
    pinned:
      reason: data-format
      notes: the built output tree a consumer ships, and the pack identity a world activates
    because:
      - r-293j8n70
      - r-jxzqhhmo
      - r-un786n7v
      - r-xcz7nzr6
      - f:a-script-module-does-not-resolve-across-packs
      - f:f-8bxtf5w7
    commentary: |
      A shared dependency's code half is bundled into the consumer already — no import reaches a
      module in another pack, so code crosses at build time or not at all. Merging puts the asset
      half on the path the code half takes, instead of leaving a dependency half vendored and half
      shipped beside.

      It is also what lets an author name a vendored asset at all. A hand-written client entity
      definition is JSON with no build-time constant to reach a namespace with; merged, the
      namespace the author chose is the one the vendored content carries, so the author writes
      their own names and the references land.

      One pack leaves one identity, so no uuid is derived from a consumer and none is minted for a
      vendoring: the output tree keeps its kind-named shape and the workspace gains no pack that no
      source declares.

  - id: d-l4wh3cgf
    title: a consumer with no pack of its own gets one
    statement: |
      a package that vendors a pack of a kind it holds no source pack of is built one, under a uuid
      derived from the package's own name. Its author declares nothing for it.
    status: proposed
    pinned:
      reason: data-format
      notes: the uuid a world activates the pack by
    because:
      - d-82zp77fo
      - r-zcdmh9p6
      - r-293j8n70
      - r-xcz7nzr6
    commentary: |
      Using a library and holding no assets of one's own is the ordinary case, and it is the one
      that should ask for the least. A package name is already unique in a workspace and already
      stable across rebuilds and checkouts, which is everything a uuid a world activates by has to
      be — so there is nothing here an author could supply that the package does not already say.

      Renaming the package changes the uuid, and a world holding the old one loses the pack. That is
      the one thing to know about it.

      Deriving here and not for a pack the author already minted a uuid for is the same rule read
      twice: the identity comes from what the package declares, and it is derived only where the
      package declares none.

  - id: d-27lzrwsu
    title: a pack declares itself vendored, and never deploys on its own
    statement: |
      a pack states in its own source that it is vendored. Every package depending on it vendors
      it, and the pack is neither deployed nor activated as a pack of its own — by the harness, in
      an archive, or anywhere else the pack set is read.
    status: proposed
    pinned:
      reason: public-api
      notes: the declaration a shared pack's author writes, and the difference between a pack that ships and one that does not
    because:
      - r-293j8n70
      - d-82zp77fo
      - f:f-8bxtf5w7
      - f:a-declared-pack-dependency-activates-the-depended-on-pack
    cases:
      - when: a pack declares itself vendored
        then: every consumer merges it, and it reaches no pool and no activation list
      - otherwise: the dependency resolves as it does today, both packs shipping
    commentary: |
      Whether a pack can be vendored is a property of the pack — it has to hold only content whose
      names the build can rewrite — so the pack is where it is declared, once, rather than in each
      consumer.

      Deploying it as well as its vendorings would put its unstamped names into the same pack stack
      as theirs, which is the cross-talk the namespace exists to prevent. It also settles what a
      declared dependency does: a depended-on pack is pulled into the stack whether or not a world
      lists it, so leaving it in the pool is enough to activate it.

  - id: d-jdqt63fi
    title: vendoring reads the vendored pack's source tree
    statement: |
      a consumer's build reads the vendored pack's source directory and completes and rewrites it
      itself. It does not read that package's built output and does not require it to have been
      built.
    status: proposed
    because:
      - d-82zp77fo
      - d-s1haiu19
      - d-vpwz0dps
    commentary: |
      A pack package's build produces its own packs and nothing else, so there is no ordering
      between two packages' builds to lean on. Reading source keeps it that way — and the script
      half, which is the part that needs building, arrives through the bundler rather than through
      the vendoring.

  - id: d-zk0c48uy
    title: the namespace is an opt-in build setting defaulting to the package name
    statement: |
      a package turns namespacing on in the build configuration it already hands the kit, and gets
      its own package name, slugged, unless it names something else. A package that vendors must
      turn it on. No field is added to `manifest.json`.
    status: proposed
    pinned:
      reason: public-api
      notes: the setting a package writes, and the names it gets when it writes only the switch
    because:
      - r-jxzqhhmo
      - r-293j8n70
      - d-qtq0ccy6
      - d-1w5rjhg6
      - r-jl42pzah
    cases:
      - when: the setting names a namespace
        then: that is the namespace
      - otherwise: the namespace is the package's own name, slugged into what an identifier may hold
    commentary: |
      A namespace is something the build consumes and nothing the engine reads, and a manifest is
      shipped verbatim to the engine. Putting it in the manifest would mean declaring and validating
      a field that has no meaning there.

      The default is the package name because it is already unique across the registry, so the
      author who thinks about none of this gets the safe answer. A short name like `rpg` is
      available to anyone who wants it and is a claim on a token strangers may also want; making it
      the thing you type rather than the thing you get is the whole of the protection available.

  - id: d-8qkamvz7
    title: a namespaced pack writes bare names in source
    statement: |
      in a pack with namespacing on, names are written bare in source — `wizard`,
      `geometry.wizard` — and the build writes the namespace into them.
    status: proposed
    pinned:
      reason: public-api
      notes: the form an author writes every name of their own in, once they turn namespacing on
    because:
      - r-jxzqhhmo
      - d-zk0c48uy
      - f:f-i8fx23h0
    cases:
      - when: namespacing is off
        then: names reach the output as the source spells them
      - when: a source name is bare
        then: the built name carries the namespace
      - otherwise: the build fails, naming the file and the name
    commentary: |
      One place states the namespace and one thing applies it, so a rename is a one-line change and
      a source name cannot claim a namespace the setting contradicts.

  - id: d-ni5n2r3c
    title: only names a pack declares are rewritten
    statement: |
      the build rewrites the names a pack declares and the references to them. A reference to a name
      the pack does not declare — vanilla content, or another pack's — is copied through untouched.
    status: proposed
    pinned:
      reason: public-api
      notes: which of an author's references survive the build as written
    because:
      - r-c9bu8xes
      - d-8qkamvz7
      - f:f-a7v7ex4x
      - f:f-z999a7yv
    cases:
      - when: a client entity names a geometry its own pack declares
        then: both the declaration and the reference carry the namespace
      - when: it names `geometry.evoker.v1.8`, or any material, texture or animation of the vanilla packs
        then: the reference is written as it was
    commentary: |
      An entity's appearance may be assembled from vanilla assets with nothing copied, so a rewrite
      that touched every name would break every such reference. What separates the two is
      declaration, which the pack's own content answers.

  - id: d-7aqne91n
    title: the content kinds a vendored pack may hold
    statement: |
      a vendored pack may declare entity identifiers, geometries, textures, materials, render
      controllers, animations, animation controllers, and the localization entries keyed by an
      entity identifier — client and behavior halves alike. Content of any other kind in a vendored
      pack fails the build, naming the file.
    status: proposed
    pinned:
      reason: public-api
      notes: what a shared pack's author may put in a pack, and what a consumer's build will accept
    because:
      - r-c9bu8xes
      - d-27lzrwsu
      - f:f-8bxtf5w7
      - f:f-09o4knzt
      - f:f-gvmc2wfa
    commentary: |
      Each of these names what it declares in a form the build can rewrite, and a localization key
      follows `entity.<id>.name` from the identifier by one rule with no exceptions, so it moves
      with the identifier.

      The list is what the evidence covers and not what a pack could contain. Items, blocks, loot
      tables, sounds, particles and the short-name atlases are outside it because nothing here
      establishes how their names resolve across a pack stack — refusing them is the honest
      reading, and each is admitted by an increment that brings the evidence.

  - id: d-bhsz9eou
    title: a namespace is claimed once among the packages a build reads
    statement: |
      one namespace is claimed by exactly one of the packages a build reads — the workspace's, and
      the dependencies it vendors from. Two claiming the same one is an error naming the namespace
      and every claimant, with no claimant preferred.
    status: proposed
    because:
      - r-293j8n70
      - r-dge04upw
      - d-zk0c48uy
      - f:f-8bxtf5w7
    commentary: |
      A shared namespace is a shared name space, so two packages picking one puts back exactly the
      contention vendoring removed — and quietly, since each builds fine alone.

      What a build reads is what it can check, and that is short of every pack a world will run.
      Two strangers who both took `rpg` collide in the world and in no build, which is what makes
      the default namespace matter more than this check does.

  - id: d-p1i76cgb
    title: a name contended for is caught at build time or not at all
    statement: |
      the kit checks names where it can see them all — at a build — and ships nothing that looks for
      a contended name at run time.
    status: proposed
    because:
      - r-293j8n70
      - d-bhsz9eou
      - d-1vhwdhk1
      - f:f-8bxtf5w7
      - f:a-resource-pack-cannot-carry-anything-a-script-can-reach
      - f:f-o8xk7go5
    commentary: |
      Not a preference — the detection does not exist. A resource pack can carry nothing a script
      can reach: it cannot be enumerated, `getPackSettings` reads empty, and a timeline in one
      cannot fire a scriptevent, so there is no canary to ship. On the behavior side
      `EntityTypes.getAll` marks no type with the pack that defined it and repeats no id, so a
      definition that lost is simply absent, and `undefined` is the only miss signal the surface
      has — indistinguishable from a type nobody ever defined.

      So the losing pack cannot know it lost and the winning one cannot know it won. Everything the
      kit can do about contention it does before the pack ships, which is why the default namespace
      is the one nobody else can hold.

  - id: d-35ovo8rl
    title: a vendored pack ships its source in its published package
    statement: |
      a package holding a vendored pack publishes that pack's source directory, and a consumer's
      build reads it from the installed dependency exactly as from a workspace sibling.
    status: proposed
    pinned:
      reason: public-api
      notes: what a shared pack's package must contain for anyone to vendor it
    because:
      - r-dge04upw
      - d-jdqt63fi
      - d-27lzrwsu
      - f:npm-install-fetches-a-package-and-its-dependencies
    commentary: |
      Vendoring reads source, so the source has to be in the tarball — a pack that publishes only
      its built output cannot be vendored by anyone. It is the assets answering to the same rule as
      the code: what a dependency offers is what it published.

  - id: d-uq46uunx
    title: a consumer installs as one file
    statement: |
      the archive a consuming package builds holds everything a world needs from that consumer,
      vendored content included. Installing a consumer is one file and one action, whatever it
      depends on.
    status: proposed
    pinned:
      reason: public-api
      notes: what an operator installs, and what another product may build a release story on
    because:
      - d-82zp77fo
      - d-xoz6pwt8
      - d-6ewrh1g3
      - r-phxrb6qn
    commentary: |
      It follows from merging plus an archive cut from the package's own output tree, and it is
      stated because it is the outcome anyone downstream actually plans against. A product that gave
      up a one-file install when it found the archive carried only its own packs can have it back,
      and should not have to re-derive that from two decisions and an archive rule.

      Nothing about the archive changes. The vendored content is in the consumer's own output tree
      by the time the archive is cut, so what already ships one mcaddon per package now ships one
      that is complete.

  - id: d-1vhwdhk1
    title: a name collision between vendored and own content fails the build
    statement: |
      where a consumer's own content and something it vendors declare the same name, or two of its
      vendorings do, the build fails and names both declarations.
    status: proposed
    because:
      - d-82zp77fo
      - d-8qkamvz7
      - f:f-8bxtf5w7
    commentary: |
      Merging puts both under one namespace, where one silently wins and whichever consumer notices
      sees an actor drawn wrong. Nothing distinguishes the author's intent here, so neither side can
      be preferred.

  - id: d-hqtqb43i
    title: what copies unchanged, and what the build writes
    statement: |
      a file under a pack's source directory reaches the output unchanged except its
      `manifest.json`, everything under `scripts/`, any file carrying a name the build rewrites, and
      any file the consumer's own content and a vendoring both contribute to, whose entries are
      composed. Dotfiles and unknown extensions copy like the rest.
    status: proposed
    pinned:
      reason: data-format
      notes: which of an author's files survive the build byte for byte
    supersedes: d-h91x9nxz
    because:
      - r-umoo5i1i
      - r-imfmv0qc
      - r-c9bu8xes
      - d-82zp77fo
      - d-ni5n2r3c
    commentary: |
      This restates the exceptions rather than adding to them, because two of the four are new and
      the value of the entry is that the list is closed.

      Rewriting a name is already not copying, so the exception is owed to the namespace whether or
      not anything is vendored. Composing is owed to merging: most pack content is one file per
      thing, where two packs contributing one path means two declarations of one name — caught
      before this decision is reached. Where the format keeps one file for the whole pack instead,
      both packs have entries in it legitimately, and one of them winning would silently drop the
      other's.

      Every file this touches is in the output tree, which the assembly owns outright. Nothing is
      written back to a source directory, so no authored file gains generated content and no
      authored directory gains an ignored file.

  - id: d-xoz6pwt8
    title: a vendored dependency leaves the built manifest
    statement: |
      the completed manifest carries no `dependencies` entry for a pack the build vendored. The
      entry stays in source, where it is what declares the dependency.
    status: proposed
    pinned:
      reason: data-format
      notes: the built manifest a world reads
    because:
      - d-27lzrwsu
      - d-82zp77fo
      - r-wi7qf6to
      - f:a-declared-pack-dependency-activates-the-depended-on-pack
    commentary: |
      There is nothing left to depend on: the content is in the consumer's own pack, and the pack
      the entry names never reaches a pool. Left in, the entry would name a uuid the world cannot
      resolve.

  - id: d-zm6pxfrg
    title: the library's namespace is an injected constant
    statement: |
      the build injects the consumer's namespace into the bundle as a constant, and a vendored
      library reads it to compose the names it calls with.
    status: proposed
    pinned:
      reason: public-api
      notes: how a vendored library reads the namespace it composes names with
    because:
      - r-tvea5tvf
      - d-zk0c48uy
      - f:a-script-module-does-not-resolve-across-packs
    commentary: |
      The library is bundled per consumer already, so the constant is per consumer without anything
      being asked of the consumer. It is the same value `d-zk0c48uy` puts in the build
      configuration, reaching the code path rather than the asset path.
