{
  "name": "test-lib-design-artifacts",
  "private": true,
  "description": "Evidence scripts for facts in ../facts.yaml. Each probe names the fact it backs.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@minecraft/server": "2.8.0",
    "@minecraft/vanilla-data": "^1.26.33",
    "typescript": "^5.5.0"
  }
}
