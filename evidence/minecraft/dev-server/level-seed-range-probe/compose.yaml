name: level-seed-range-probe
services:
  bedrock:
    image: itzg/minecraft-bedrock-server
    environment:
      EULA: "TRUE"
      LEVEL_NAME: dev
      SERVER_NAME: level-seed-range-probe
      LEVEL_SEED: ${LEVEL_SEED-}
    volumes:
      - data:/data
volumes:
  data:
