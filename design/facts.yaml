# Global facts — discovered invariants, true across the whole project.
# Schema: id, claim, backing (tested|documented|assumed),
#         status (active|superseded), sources, caveat (optional),
#         superseded_by (required if superseded).
# A source needs exactly one locator: url + where, or description.
# Cited from prose as [[fact:<id>]].
#
# Empty: nothing has earned promotion yet. An entry graduates from an area to here
# once designs in a *second* area depend on it, not because it feels universal.

[]
