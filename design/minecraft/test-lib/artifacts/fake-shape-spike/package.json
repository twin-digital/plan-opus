{
  "name": "test-lib-fake-shape-spike",
  "private": true,
  "description": "TypeScript spike for the fake-shape options: declaration merging, proxies, generated stubs. Compile-time probes are *.ts checked by `npm run check`; runtime probes are *.mjs.",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit"
  },
  "devDependencies": {
    "@minecraft/server": "2.8.0",
    "typescript": "^5.5.0"
  }
}
