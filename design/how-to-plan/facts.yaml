# Area facts — discovered invariants shared by every design in the planning area.
# Schema: id, claim, backing (tested|documented|assumed), sources,
#         status (active|retired, default active), caveat (optional),
#         reason (required if retired), superseded_by (required if reason is superseded).
# A source needs exactly one locator: url + where, or description; every in-repo url is
# repo-root-relative and every quote is a block scalar.
# Cited from prose as [[f:<id>]] — resolved after the design's own file.
#
# An entry promotes here when a second design in this area depends on it.

[]
