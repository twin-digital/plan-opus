# Area facts — discovered invariants shared by every design in the planning area.
# Schema: id, claim, backing (tested|documented|assumed),
#         status (active|stale|superseded), sources (optional), risk (optional),
#         test (optional), superseded_by (required if superseded).
# A source needs exactly one locator: url + where, or description. See design #0.
# Cited from prose as [[fact:<id>]] — resolved after the design's own file.
#
# An entry promotes here when a second design in this area depends on it.

[]
