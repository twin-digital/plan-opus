# Requirements scoped to the harness design.
# Promote to design/how-to-plan/requirements.yaml when a second design depends on one.

- id: node-for-tooling
  statement: permanent scripts and tooling are written in Node.js unless the task requires otherwise
  force: soft
  status: active
  sources:
    - description: standing personal convention, applied across all projects
