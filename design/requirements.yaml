# Global requirements — owner fiat, true across the whole project.
# Schema: id, statement, force (hard|soft), status (active|retired), rationale (optional).
# rationale is always a block scalar. Requirements take no sources: they are decided,
# not discovered, so there is nothing to cite.
# Cited from prose as [[r:<id>]].
#
# Empty: nothing has earned promotion yet. An entry graduates from an area to here
# once designs in a *second* area depend on it, not because it feels universal.

[]
