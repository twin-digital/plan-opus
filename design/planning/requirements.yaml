# Area requirements — owner fiat binding every design in the planning area.
# Schema: id, statement, force (hard|soft), status (active|retired), sources (optional).
# A source needs exactly one locator: url + where, or description. See design #0.
# Cited from prose as [[req:<id>]] — resolved after the design's own file.
#
# An entry promotes here when a second design in this area depends on it.

[]
